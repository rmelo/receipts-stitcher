version: '3'
services:
  web:
    image: stitcher
    container_name: flask
    build: .
    environment:
      - FLASK_DEBUG=1
    networks:
      my-network:
        aliases:
          - flask-app
          
  nginx:
    image: nginx:1.13.7
    container_name: nginx
    ports:
      - 80:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    networks:
      - my-network

networks:
  my-network: